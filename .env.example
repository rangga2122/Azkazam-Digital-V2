###############################
# Contoh .env untuk Deployment #
###############################

# Client-side (terbaca oleh browser)
# Pastikan DISAMAKAN di dashboard deploy (Vercel/Netlify) pada Environment Variables
VITE_SUPABASE_URL="https://YOUR-PROJECT-ID.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR-ANON-PUBLIC-KEY"

# Server-side (hanya di backend/edge function)
# WAJIB diset di platform deploy dan JANGAN pernah dikirim ke client
SUPABASE_URL="https://YOUR-PROJECT-ID.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="YOUR-SERVICE-ROLE-SECRET"

# Opsional (dipakai reset harian & cron)
RESET_TZ="Asia/Jakarta"
CRON_SECRET="change-this-to-a-random-secret"

# Opsional (AI provider)
# Server-side: dipakai untuk generate video
GEMINI_API_KEY=""
# Client-side: dipakai untuk TTS (Text-to-Speech) dan analisa gambar di Editor Video & Narasi
# Harus diawali dengan VITE_ agar bisa diakses di browser
VITE_GEMINI_API_KEY=""

# Catatan:
# 1) Di Vercel, set di Project Settings → Environment Variables untuk Production & Preview.
# 2) Setelah mengubah variabel, lakukan redeploy agar environment terambil.
# 3) SERVICE_ROLE_KEY hanya di server. Jangan expose ke client atau file publik.

# Sora2 Video Generator
# Server-side: API key untuk 2Captcha solver
SORA_CAPTCHA_API_KEY=""
# Opsional: Override default captcha settings
SORA_CAPTCHA_SITE_KEY="0x4AAAAAACDBydnKT0zYzh2H"
SORA_CAPTCHA_SITE_URL="https://geminigen.ai/app/video-gen/"
# Opsional: Custom Google Sheet URL untuk token bearer
SORA_BEARER_SHEET_CSV_URL=""
